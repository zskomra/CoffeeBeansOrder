import AvailableProduct from "../../AvailableProduct";

const AvailableAccessories = (props) => {
    const productName = "accessories";
    return (
        <AvailableProduct productName = {productName} />
    );
}

export default AvailableAccessories;